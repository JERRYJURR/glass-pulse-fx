// Main entry: the React component + everything from the vanilla core.
// Import from 'glass-pulse-fx/core' instead if you don't want React in the bundle.

export * from './core';
export { GlassFx } from './GlassFx';
export type { GlassFxProps } from './GlassFx';
